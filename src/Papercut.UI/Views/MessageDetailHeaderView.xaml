<UserControl x:Class="Papercut.Views.MessageDetailHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="600"  Background="{DynamicResource MahApps.Brushes.ThemeBackground}">
    <Grid>
        <Border BorderBrush="{DynamicResource MahApps.Brushes.Control.Border}" BorderThickness="1">
        <avalonEdit:TextEditor
                xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
                Name="HeaderEdit"
                Foreground="{DynamicResource MahApps.Brushes.Text}"
                Background="{DynamicResource MahApps.Brushes.ThemeBackground}"
                Padding="4"
                FontFamily="Consolas"
                FontSize="9pt" IsReadOnly="True" />
        </Border>
    </Grid>
</UserControl>
