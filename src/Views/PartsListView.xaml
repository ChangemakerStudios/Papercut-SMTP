<UserControl x:Class="Papercut.Views.PartsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="300">
    <Grid>
        <ListBox x:Name="MessagesList" Width="Auto" Height="Auto"
    		VerticalContentAlignment="Stretch" HorizontalAlignment="Stretch" Padding="0"
    		ItemsSource="{Binding Parts}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding ContentType}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding FileName}" Visibility="{Binding FileName,Converter={StaticResource CollapsedIfFalse}}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <!--<ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                </Style>
            </ListBox.ItemContainerStyle>-->
        </ListBox>

    </Grid>
</UserControl>
